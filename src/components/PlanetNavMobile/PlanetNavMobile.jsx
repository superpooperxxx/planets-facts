import './PlanetNavMobile.scss';

const planetNavBtns = ['Overview', 'Internal structure', 'Surface geology'];

export const PlanetNavMobile = () => {
  return (
    <ul className="Planet-Nav-Mobile">

    </ul>
  );
};
